let isDone: boolean = false;

isDone = true;

console.log(typeof isDone); // 'boolean'

// npx tsc ./2.BasicTypes/Types/boolean.ts

let isOk: Boolean = true;

// let isNotOk: boolean = new Boolean(true)  레퍼 객체가 생성되기 때문에 원시 값은 부울린에 할당을 못함
